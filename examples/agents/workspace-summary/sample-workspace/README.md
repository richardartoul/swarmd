# Demo Workspace

This sample tree exists so the `workspace-summary` agent example has something deterministic to inspect.

It represents a small fictional service with:

- an application deployment manifest
- a shell deployment script
- a short follow-up note list
