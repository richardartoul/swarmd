# Follow-Up Notes

- Replace the default image tag in CI before production rollout.
- Add rollback instructions to the deployment script.
- Review how runtime secrets are injected into the deployment.
